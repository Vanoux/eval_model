{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'src/Controller/AlumnisController.php'|file_link(0) }}">src/Controller/AlumnisController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'templates/alumnis/index.html.twig'|file_link(0) }}">templates/alumnis/index.html.twig</a></code></li>
    </ul>
</div>

<section class="posts">

    {% for post in posts %}

    <articles>
        <h2>{{ post.titre }}</h2>
        <div class="content">
            <h4>{{ post.categorie }}</h4>
            {{ post.date}}
            {{ post.contenu | raw }}
            {{ post.user }}
        </div>
    </articles>
    {% endfor %}



</section>
{% endblock %}
